package utils 
{
	import flash.display.DisplayObject;
	import gs.TweenLite;
	
	/**
	 * Effects
	 * @author Bertrand Niquel
	 */
	
	public class Effects
	{
		
		//public static function blink(target:DisplayObject, frequency:Number, times:Number):void
		//{
			//
			//blink_on(target, frequency);
			//TweenLite.to(target, 0, { autoAlpha: 1, onComplete: Effects.blink_off, onCompleteParams: [target, frequency], delay: frequency } );
		//}
		//
		//
		//public static function blink_on(target:DisplayObject, frequency:Number):void
		//{
			//TweenLite.to(target, 0, { autoAlpha: 1, onComplete: Effects.blink_off, onCompleteParams: [target, frequency], delay: frequency } );
		//}
		//
		//
		//public static function blink_off(target:DisplayObject, frequency:Number):void
		//{
			//TweenLite.to(target, 0, { autoAlpha: 0, onComplete: Effects.blink_off, onCompleteParams: [target, frequency], delay: frequency } );
		//}
		
	}

}